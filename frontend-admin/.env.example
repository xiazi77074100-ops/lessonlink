# port 8001 matches docker-compose.yml's backend service (mapped away from
# the default 8000 to avoid clashing with other local projects); adjust if
# you're running the backend directly with uvicorn on 8000 instead.
VITE_API_BASE_URL=http://localhost:8001/api/v1
VITE_PARENT_APP_URL=http://localhost:5174
